# Learned Patterns

File này được cập nhật tự động bởi `/learn` command sau mỗi session.
Chứa các patterns, conventions, và insights được rút ra từ thực tế làm việc với project.

**Dùng như thế nào:** Claude tự động đọc file này trong `/autopilot` để áp dụng patterns khi implement.

---

<!-- Các patterns sẽ được thêm vào đây bởi /learn -->
