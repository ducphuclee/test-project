# Component Catalog

> Generated by /business-bootstrap.
> Refresh: chạy `/business-bootstrap` khi có component mới.

---

## Status

**Project Type:** Configuration & Workflow (no UI components yet)

This project is a validation tool for the Claude ecosystem's dual-role workflow. No UI components exist at this stage.

### Future

When application code is added (frontend with React/Next.js), business member should:
1. Run `/business-bootstrap` to scan components
2. Use `/design` command to build prototypes
3. Use component catalog to ensure consistency

### Design System

**Current:** Not applicable (no UI project)

When frontend is added, refer to:
- `.project-info/conventions/design.md` (if created)
- `.claude/skills/frontend-design/SKILL.md` (design patterns)

---

## Next Steps

For design & prototyping work:
1. Run `/design [description]` to create UI mockups
2. Use `/deploy` to preview
3. Use `/handoff` to prepare artifacts for developers
